<div class="main user-show">
  <div class="container">
    <div class="user">
      <img src="<%= "/users_images/#{@user.image_name}" %>">
      <h2><%=@user.name %></h2>
      <p><%=@user.email %></p>
      <%= link_to("Editing", "/users/#{@user.id}/edit") %>
    </div>
  </div>
</div>
